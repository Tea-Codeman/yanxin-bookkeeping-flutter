# 走查报告 — F7.14 新手引导（7 页全屏导览）

> 对象：`v0.7.14` · 日期 2026-09-25 · 执行：AI 经 adb 全包
> 设备：MuMu 12 · `127.0.0.1:16384` · **900×1600 @320dpi**（逻辑 450×800）
> 需求（用户）：「让第一次使用的用户知道一些没有明显标识的按钮是做什么的」
> 截图存 `.workbuddy/shots/f714-*.png`（不入库）

## 1. 装包与新鲜度核验

| 项 | 值 |
|---|---|
| 构建链路 | `python tool/build_kernel_fallback.py`（30.1s）→ `./gradlew assembleDebug -x compileFlutterBuildDebug`（19s，BUILD SUCCESSFUL） |
| APK | 207.5 MB / `09-25 14:19:14` |
| kernel sha256[:16] | APK 内 **`afc78eb45fbf097f`** vs 盘上 **`afc78eb45fbf097f`** → **新鲜度 OK** |
| 关键字 grep | `新手引导` / `认识一下颜芯记账` / `藏起来的手势` / `开始记账` **均命中** → 确认装的是本批 kernel |
| 安装 | `adb install -r -t` → `Success` |

> ⚠️ **本轮踩坑（已回写 skill）**：首次 `pm clear` 后抓语义树，报出的是**上一次走查的界面**
> （首页 + 旧流水 `286.88`），一度看着像「首启没弹引导」。根因 = `uiautomator dump` 写到固定
> `/sdcard/_ui.xml`，在 App 切换的瞬间 `cat` 拿不到新文件 → 读到陈旧内容。
> **改用独立文件名落盘 + 截图交叉验证**后确认引导页正常弹出。
> 判据：语义树与数据库状态**互相矛盾**时（库里 `transactions=0`、屏幕上却有流水），先怀疑落盘缓存。

## 2. 逐项结果

| # | 验收项 | 操作 | 结果 |
|---|---|---|---|
| D1 | **全新安装首启自动弹** | `pm clear` → 冷启动（库内无 `onboarding_done`、无 `active_book_id`） | ✅ 全屏引导盖住首页，见「认识一下颜芯记账」+ 7 点进度 + 「跳过」+「下一步」（`f714-01-launch.png`） |
| D2 | 顶部进度 + 跳过 + 下一步 | 语义树逐页取节点 | ✅ 7 个进度点（当前页为拉长琥珀胶囊 + 墨色描边）；「跳过」常驻右上；底部按钮常驻 |
| D3 | **7 页内容与 SPEC §3.2 逐条吻合** | 点「下一步」6 次，每页截图 + 语义树 | ✅ 见下表 §3 |
| D4 | 末页「开始记账」→ 关闭 + 写标记 | 点末页按钮 | ✅ 回到首页；`schema_meta` 落 `onboarding_done|1` |
| D5 | 已看过 → 冷启动不再弹 | `force-stop` → 冷启动 | ✅ 直接进首页 |
| D6 | **「我的 → 新手引导」可随时重看** | 我的页点首位条目 | ✅ 进入第 1 页，行为与首启一致 |
| D7 | 引导**只读**、不碰业务数据 | 引导前后查库 | ✅ `0 tx / 1 acct / 15 cat` 前后**完全一致** |
| D8 | **系统返回也写标记** | 从「我的」进引导 → `keyevent 4` | ✅ 回到「我的」页；`onboarding_done|1` 已落库 |
| D9 | **老用户不弹**（用户裁定 ④） | 删 `onboarding_done`、保留 `active_book_id` → 冷启动 | ✅ **不弹**；且启动后**未把标记写回**（仍是「没看过」状态，符合设计） |
| D10 | 矮视口不溢出 | `wm size 900x1000`（逻辑 450×500）→ 进引导 | ✅ 「跳过 / 标题 / 正文 / 下一步」全部可见、无裁切（`f714-short.png`）；验完 `wm size reset` |

- **崩溃**：`logcat -b crash` 中本 App 记录数 = **0**；全程无 Flutter 异常 / `GoException`。

## 3. D3 逐页核对（截图 + 语义树）

| 页 | 标题（= SPEC §3.2） | 示意图可读元素 | 结果 |
|---|---|---|---|
| P1 | 认识一下颜芯记账 | 猪头吉祥物圆卡 + 星形装饰 | ✅ `f714-01-launch.png` |
| P2 | 中间歪着的方块 = 记一笔 | 迷你底栏（首页/日历/资产/我的）+ 中央琥珀方块**高亮圈** + 「记一笔」折线标注 | ✅ `f714-p2.png` |
| P3 | 右上角这三个图标 | 迷你 header（默认账本 + 放大镜/单据/柱状图） | ✅ `f714-p3.png` |
| P4 | 月份可以往回翻 | 迷你琥珀卡（`‹ ›`、本月支出 `1,286.00`）+ 迷你预算卡（铅笔、`¥2000.00`），两处 Callout | ✅ `f714-p4.png` |
| P5 | 三个藏起来的手势 | 三行手势卡：长按日历格 / 月历左右滑 / 流水行左滑露出红色「删除」 | ✅ `f714-p5.png` |
| P6 | 还有两个页面的用法 | 迷你资产页（`+` / 现金 `¥286.88`）+ 迷你报表三档（明细 / 分类 / 账户） | ✅ `f714-p6.png` |
| P7 | 可以开始记账了 | 「我的 → 新手引导」胶囊 + 按钮**变为「开始记账」** | ✅ `f714-p7.png` |

## 4. 结论

- D1–D10 **全部通过**，无回归、无崩溃；用户裁定的四条（全屏分页导览 / 内容四项全覆盖 → 7 页 /
  「我的」入口 / **老用户不弹**）均在真机一一验证。
- 「老用户不弹」的判据（`active_book_id` 有值）经**造数据实测**：删掉标记后冷启动确实不弹，
  且**不会**被误写标记 —— 老用户仍可从「我的」主动看。
- 「看过」标记由 `dispose()` 一处收口：**跳过 / 开始记账 / 系统返回**三条路径均已实测落库。

### 遗留观察（不影响交付，未改）

1. **页面下方留白较大**：示意图 + 标题 + 正文总高度小于可用区，内容统一靠上排（`SingleChildScrollView`
   顶部对齐）。视觉上「上紧下松」，但矮视口下正是靠这个换来了不溢出。属设计取舍，本轮未动。
2. **视觉对拍未做**：页面原型 `D:\new file\modao\yanxin\` 的 `screens.js` **没有引导屏**，
   属 F7.14 全新的页面形态 → 无对应原型可比对，非跳过。
3. **真机（非模拟器）未验**：本轮为 MuMu 12 模拟器全包走查。
