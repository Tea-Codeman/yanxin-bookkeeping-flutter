# 走查报告 — F7.13 统计页图表绘制修复（分类圆环 / 趋势柱）

> 对象：`v0.7.13` · 日期 2026-09-25 · 执行：AI 经 adb 全包
> 设备：MuMu 12 · `emulator-5554` · **900×1600 @320dpi**（逻辑 450×800）
> 触发：用户报「统计页面中的统计图样式有问题」 → 定位 → 修复 → 用户复核通过
> 截图存 `.workbuddy/qa-stats/`（不入库）

## 1. 装包与新鲜度核验

| 项 | 值 |
|---|---|
| 构建链路 | `python tool/build_kernel_fallback.py`（17.6s）→ `./gradlew assembleDebug -x compileFlutterBuildDebug`（11s，BUILD SUCCESSFUL） |
| APK | 207.5 MB / `09-25 11:56:39` |
| kernel sha256[:16] | APK 内 **`93c46033f24c680e`** vs 盘上 **`93c46033f24c680e`** → **新鲜度 OK** |
| 关键字 grep | `trendBarHeight` / `kTrendBarWidth` 均命中 → 确认装的是本批 kernel |
| 覆盖安装 | `adb install -r` → `Success`（用户数据保持：修复前后统计页支出均为 **286.88**） |

> ⚠️ 本轮踩坑：`gradlew` 必须在 `source env.sh` 之后跑 —— 否则 `GRADLE_USER_HOME` 缺失，
> Gradle 去用 `C:\Users\panda\.gradle` 的默认目录并直接报
> `journal-1.lock (拒绝访问。)`，2 秒即 BUILD FAILED（表现像「构建坏了」，实为环境变量没注入）。

## 2. 分类占比圆环（A 项）

### 修复前（`03_stats.png` / `pie_zoom.png`）

- 每一瓣都从**圆心**射出一条实心楔形：橙 / 粉 / 青 / 蓝 / 紫 五瓣叠加成**风车状**；
- 圆心文字 `286.88` 与「合计（元）」被楔形覆盖，可读性受损；
- 首尾接缝（紫 ↔ 橙）明显比其他接缝宽。

### 修复后（`04_fixed.png` / `pie_zoom_fixed.png`）

| 走查项 | 结果 |
|---|---|
| D1 圆环形态 | ✅ 干净的环形描边，圆心留白，无任何楔形 / 辐条 |
| D2 圆心文字 | ✅ `286.88` + 「合计（元）」完整可读，四周无遮挡 |
| D3 接缝均匀 | ✅ 5 处接缝等宽（含首尾收口处），不再有豁口 |
| D4 图例颜色对应 | ✅ 餐饮橙 / 人情粉 / 娱乐青 / 居住蓝 / 交通紫，与环上色块一一对应 |
| D5 数据自洽 | ✅ 图例 88.88 + 80.00 + 74.00 + 33.00 + 11.00 = **286.88** = 圆心合计；占比 31.0 + 27.9 + 25.8 + 11.5 + 3.8 = **100.0%** |

## 3. 近 6 个月趋势柱（B 项）

### 修复前（`04_fixed.png`）

4 / 5 / 6 / 7 月**整月无流水**，却各画出一对 2px 内高的基座胶囊 —— 视觉上像「每个月都有小额收支」。

### 修复后（`05_bars_fixed.png` / `bars_before_after.png`）

| 走查项 | 结果 |
|---|---|
| D6 零值月不画柱 | ✅ 4 / 5 / 6 / 7 月位置完全空白，无胶囊 |
| D7 有数据柱零漂移 | ✅ 8 月（红 1 根 + 绿 1 根）、9 月（红 1 根）的**位置与高度与修复前逐像素一致** |
| D8 9 月收入柱 | ✅ 收入 0.00 → 同样不画（符合「无数据不画」口径，仅剩红柱） |
| D9 月份标签完整 | ✅ 4–9 月 6 个标签仍按位铺开，无数据月份只少了柱子、位置不塌 |

## 4. 结论

- A / B 两处修复在真机（覆盖安装、真实用户数据）上**全部通过**，无回归、无崩溃；
- 圆环的绘制口径已用单测锁死（`test/features/stats/category_pie_test.dart`：
  `useCenter == false` + 接缝 `closeTo(kDonutGap, 1e-9)`）；
  趋势柱高度策略同理（`test/features/stats/trend_bars_test.dart`）。
- 遗留观察：`_Bar` 的 `height + 4`（描边补偿）在柱高触顶时会被父级 `SizedBox(height: 96)`
  约束削掉 4px → 最大柱实际显示 96 而非 100。影响 ≤ 4%（柱区 96 的 4%），本轮未动。
