
## F5 账单导入（2026-09-10 凌晨）

- 提交 `0c82620`，已推远端。M2 等价代码层全部落地，剩真机验收 8 项。
- 五层移植：decode（utf8 严格→gbk 回环校验）、csv 状态机、profiles、
  normalize（表头定位/转分/Excel 序列号）、xlsx（archive+正则，弃 excel 包保精度）、
  categorize（39 条规则）、importer（drift 单事务+dryRun 哨兵回滚）。
- 真实件对拍：微信 xlsx 335 笔→导入 327（8 条退款黑名单跳过）；支付宝 GBK
  32 笔→导入 28；31 位交易单号不丢精度。门禁 143/143 + analyze 0 issue + APK 过。
- **坑 7**：Kotlin 增量编译跨盘崩溃——pub 缓存(C:)源码 vs 项目(D:)，
  android_file_picker 编译报 "different roots" → gradle.properties 加
  `kotlin.incremental=false`。装了新 Flutter 插件后 APK 构建挂这就要想这招。
- **坑 8**：gbk_codec 0.4.0 全系 sdk 上界 <3.0.0 → dependency_overrides 放行；
  其 gbk_bytesDecode 对非法序列静默回退不产 U+FFFD → 用重编码回环校验判坏件。
- **坑 9**：file_picker 12 改静态 API（FilePicker.pickFile + readAsBytes，
  withData 已废弃）；CheckboxListTile 新版删了 trailing 用 secondary。
- 导入页测试要点：currentBookProvider 必须 await .future（read().value
  在首次构建中为 null）；drift 事务真实异步 → pumpUntil 等对话框。
- 下一步：F5 真机验收（M2 等价 8 项）→ F6 收尾。

## 环境体检 + 交接（用户清理文件后，2026-09-11 00:0x）
- 结论：**依赖与环境配置无丢失**。flutter analyze 0 issue（81.7s）、
  flutter test 143/143、APK 已存在、.gradle-home 4.1GB 完好、pub 缓存双源并存。
- 唯一真实缺陷：`env.sh` 的 `fx-test`/`fx-qa` 用 `env -u ... "PROGRAMFILES(X86)=..."`
  前缀 → 沙箱里 **env 被 safe-bin shim 吞掉，零输出 0.5s 返回**。
  已改为 bash 内建 `unset` + 子 shell，并去掉 PROGRAMFILES(X86) 注入
  （sqlite3 2.9.4 不需要）。冒烟验证 fx-test 通过。
- **坑 10**：不 `source env.sh` 直接 `flutter pub get` → pubspec.lock 里 113 个包的
  url 被改写成 pub.dev。修复：`source env.sh && flutter pub get` 即还原。
  现 lock 仅剩 android_file_picker 1.1.0→1.1.1（传递依赖，无害）。
- 本机有**两个同版本 Flutter**（D:\Download 与 D:\Downloads，皆 3.47.2）；
  `android/local.properties` 的 flutter.sdk 指向 D:\Downloads，与 env.sh 不一致但无害。
- HANDOFF.md 已按 generate-handoff-document 结构重写（含上述新坑 + 未提交改动清单）。
