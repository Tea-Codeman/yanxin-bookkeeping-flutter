# 2026-09-11

## 双 Flutter SDK 冗余排查（用户提问：要不要删一个）
- 机器上有两个**完全相同**的 SDK：
  - A `D:\Download\Flutter\flutter` — 3.03 GB / 18829 files
  - B `D:\Downloads\Flutter\flutter` — 2.29 GB / 18396 files
  - 均为 Flutter 3.47.2 stable、revision d3b14c87690、Dart 3.13.2、2026-08-26。
- 引用面（确认无外部依赖）：
  - Windows 系统/用户 PATH **都没有** flutter 条目 → 删任一都不影响其他软件。
  - 全盘只有本项目 `android/local.properties` 引用了 SDK（指向 B）。
  - `env.sh` 的 PATH 指向 **A**。
- **隐患**：CLI（pub/test/analyze）用 A，Gradle 读 local.properties 用 B → 一旦只升级其中一个就会出现「Dart 与 engine hash 分裂」的怪错。
- 已修：`android/local.properties` 的 `flutter.sdk` 改为 A（该文件 gitignore、由 flutter 工具自动重写，零风险）。
- 结论：建议删 B 保留 A（A 是 env.sh 在用、且缓存更全）。但 B 在 `D:\Downloads` 用户下载目录且含 1.8 万文件，批量删除会被 safe-delete shim 拦截 → **交给用户手工删**。

## 两个 Android 目录比对（不是重复 SDK！）
- `D:\Download\Java\Android` = **真正的 Android SDK**，10.04 GB / 37303 文件，组件齐全：
  platforms 35+36、build-tools 36.0.0、ndk 28.2.13676358、cmdline-tools/latest、cmake 3.22.1、
  platform-tools(adb)、emulator、system-images(android-35 / android-37.0)、licenses 已接受 7 个。
- `D:\Downloads\Android` = **不是 SDK**，只有一个 `jbr`（JetBrains Runtime）子目录，69 MB / 19 文件
  （bin 17 个 + lib/modules 54MB + jrt-fs.jar）。判定为 **Android Studio 被卸载/清理后的残留**
  （目录 mtime 2026-09-10 23:55 = 用户清理时刻；jbr 内文件日期 2026-07-27 = 安装日期）。
- 引用面：`ANDROID_HOME=D:\Download\Java\Android`（正确）；机器上**没有安装 Android Studio**
  （Program Files 下只有 Google\Chrome）；无任何配置引用 `D:\Downloads\Android` → 删它零风险。
- 注意：`JAVA_HOME` 默认是 JDK 25，构建必须靠 `source env.sh` 覆盖成 JDK 17。

## 环境坑（新）
- **bash 工具偶发 PATH 丢失**：报 `dirname: command not found` / `ls: command not found`。
  解：命令前加 `export PATH="/usr/bin:/bin:$PATH"`。
- **PowerShell 工具本会话 stdout 全空**（Write-Output 不回显）→ 需要取结果时改用 Bash 或 Python。

## 交接进度确认（00:31，接 HANDOFF.md）
- HEAD `0c82620` == `git ls-remote origin master` → 远端已同步，`[gone]` 确认是沙箱假象。
- 门禁复跑：**analyze 0 issue + `flutter test` 143/143 全绿**（22s）。
  ⚠️ 首次复跑出现 `bill_normalize_test.dart: Connection closed before test suite loaded`（119+1），
  单跑该文件 24/24 通过、全量复跑 143/143 → **沙箱 isolate 偶发崩溃 flake，非代码问题**。
  判据：同一文件单跑绿 + 全量复跑绿，且失败文案是 "Connection closed" 而非断言失败。
- 🔴 **Android SDK 被删（P0 阻塞）**：`D:\Download\Java\Android` 不存在，
  `flutter doctor` 报 `ANDROID_HOME = D:\Download\Java\Android but Android SDK not found`。
  SDK 完好躺在 D 盘回收站：`$RECYCLE.BIN\S-1-5-21-...\$R0X47RA`，**10.04 GB，删除时间 2026-09-11 00:27:07**
  （即本次会话前 4 分钟，疑似用户清理磁盘时误删）。内容齐全：platforms 35/36、build-tools 36.0.0、
  platform-tools(adb)、cmdline-tools、ndk、emulator、licenses、system-images。
  恢复方式：资源管理器"还原"，或 `Move-Item` 回 `D:\Download\Java\Android`。
  不恢复 → `flutter build apk` / `adb` / F5 真机验收全部做不了。
- ✅ **已解决（00:38）**：`mv '/d/$RECYCLE.BIN/S-1-5-21-...-1001/$R0X47RA' '/d/Download/Java/Android'`
  同盘 mv = 秒级改名，无复制开销。`flutter doctor` 恢复 `[√] Android toolchain ... (Android SDK version 36.0.0)`。
  注意 bash 里 `$RECYCLE` 会被当变量吞掉 → **回收站路径必须用单引号**。
  残留 `$I0X47RA` 元数据（78B）不影响使用。
- ⚠️ 还原后 `adb devices` 为空 → Redmi K50 无线 adb 需用户重连（`adb connect <ip>:5555`）。
- ✅ **APK 重编成功（01:08）**：`flutter build apk --debug` 277.8s，产物 **177,484,448 B**
  （旧包 169,377,888 B，变大 8MB 系 gradle 缓存重建差异，非异常）。真机验收可直接装这个包。
- HANDOFF.md 已同步更新：SDK 误删/还原事件、APK 新字节数、platforms 实为 35/36、
  `.trash-*` 已消失、README 进度表过期待修。

## MuMu 模拟器跑通（01:19–01:24）
- MuMu 12 安装于 `D:\Downloads\MuMu\MuMuPlayer`，运行时监听 **127.0.0.1:7555（兼容）/16384（主）**。
- SDK adb 1.0.41 (37.0.1) 可直连：`adb connect 127.0.0.1:16384` → 设备名 `emulator-5554` 或 `127.0.0.1:16384`。
- 设备信息：**x86_64 / Android 15 / model V2364A**（伪装 vivo）。
- ⚠️ **沙箱每次 Bash 调用结束都会杀 adb daemon** → 新调用里必须先 `adb connect` 再操作，
  且偶发 `device offline`（daemon 握手未完成），**同一次调用内 connect → 操作即可**。
- debug APK 含 x86_64（`flutter build apk` 默认 fat APK：arm64+armv7+x86_64）→ MuMu 直接装。
  `adb install -r -t`（**-t 必须**，debug 包 testOnly）。
- ✅ **颜芯记账已在 MuMu 跑通**：`com.teacodeman.yanxin/.MainActivity`，Impeller Vulkan 渲染，无崩溃。
  首页深色主题+琥珀 hero+预算卡（静态演示数据 101.52/10.2%）+底栏 4tab 全部正常。截图 `.workbuddy/shots/mumu-01-home.png`。
- 已 push 真实回单样本到模拟器 `/sdcard/Download/`：`wechat-sample-real.xlsx`(34KB)、`alipay-sample-real.csv`(6.7KB)
  → **F5 真机验收（M2 8 项）可直接在 MuMu 上做**，不必连 K50。

## 首次使用验收（first-run-acceptance，M1/M2 实走，01:38）
- 方法：`adb shell pm clear` 归零 → 真实点击 → `uiautomator dump --compressed` 抓 Flutter semantics 树。
  ⚠️ **本会话模型读不了 png**，看截图无用 → 必须靠 uiautomator 文本树判断界面。
  复用脚本：`.workbuddy/ui_dump.py`（自带 adb connect，输出「[可点] 文案 @(x,y)」列表，直接拿坐标去 tap）。
  坑：bounds 是 `[24,61][1378,107]`，`.replace(",", " ")` 才能 split 成 int。
- **M1 ✅ 能独立跑通（0 阻断）**；**M2 ❌ 1 阻断**。报告 `docs/acceptance-M1-M2.md`。
- **P1（阻断）导入后首页看不到数据**：导入 327 笔 7 月数据 → 点「好的」回「我的」页 →
  首页 9 月无变化、无翻月引导。数据其实对（7 月 417.92）。缺 `jumpToMonth` + 「去看账单」按钮。
- **P2（阻断）预算卡假数据**：`budget_card_placeholder.dart` 硬编码 101.52/10.2%/898.48/10.15/42.78，
  与 hero 真实支出矛盾（记 88.88 后预算卡纹丝不动）。记账 App 的信任级问题。
- P3 二次导入「导入完成 / 成功导入 0 笔」文案矛盾（且 0 新增仍报 167 笔未匹配）；
  P4 首页空态无引导；P5 保存按钮在折叠下方（横屏需滚动）；P6 预览页看不出如何取消单条。
- **已验证正确**：微信 335→327（8 退款黑名单）、支付宝 GBK 32→28、中文无乱码、
  **二次导入 0 新增/327 重复跳过（指纹幂等）**、导入 <1s、force-stop 后数据仍在、SAF 免权限、默认账本+预置分类自动入库。
- 回写：HANDOFF.md 新增「首次使用验收」节 + README 开发规范引用报告。
- `.trash-*` 残留目录**已不在**（HANDOFF 的 P2 已自然消失）。`.gradle-home` 完好、APK 169MB 在位。
- README.md 的「迁移进度」表**严重过期**（F1 🟨 / F2–F6 ⬜，实际 F1–F5 代码全完成），
  且技术选型表写「xlsx → excel 包」但实际是 `archive` + 手写正则 → F6 需一并修。
- HANDOFF「已确认事实」里 platform 写 35/36/37，实际只有 **35/36**。

## P1/P2 修复 + 端到端验证（debugging-and-error-recovery，02:0x–02:2x）
- **P1 修**：`ledger_controller.dart` 加 `jumpToMonth(year,month)`（取 `state.value ?? await future`，
  不受 `canGoNext` 限制，可跳历史月）；`import_page.dart` 报告框「好的」拆为「完成」+「去看账单」，
  正文加「其中 N 笔属于 YYYY-MM，点「去看账单」直接翻到该月」，新增 `_latestMonthOf()` 取未取消行最晚月份。
- **P2 修（用户选方案 a：示例标注，不接真实数据）**：`budget_card_placeholder.dart` 标题旁加 `_SampleChip`
  「示例」+ 卡底灰字「预算功能建设中，以上均为示例数据，不代表你的真实消费」。数字仍写死，预算功能实现时移除。
- **连带修**：`test/widget_test.dart` 长按删除前补 `ensureVisible`（预算卡变高把 800×600 视口列表项挤出屏幕）。
- 新增 3 测试：`ledger_controller_test.dart`(jumpToMonth)、`budget_card_test.dart`、`import_page_test.dart` 更新。
- 门禁：analyze 0 issue、**flutter test 146/146**（原 143 + 3）。
- **端到端验证（MuMu，新包 213.9MB @02:15）**：P2「示例」chip + 说明文案可见；
  P1 报告框两按钮 + 月份引导语齐全；点「去看账单」→ 落首页 + 自动翻到 **7 月（支出 417.92）**，列表首条「7月16日」可见。两阻断闭环。
- 回写：`docs/acceptance-M1-M2.md` §9 补「端到端验证（MuMu）」表；HANDOFF 验收节 + 顶行时间已更新。
- ⚠️ 会话中途撞模型频率限制（429，05:00 UTC+8 重置），切换模型后续跑完成。

## P3–P6 修复 + 冒烟 + F6 文档收尾（02:23–02:55）
- 提交 `8172737`（P1/P2 + env.sh + acceptance 报告 + 走查脚本；`.gitignore` 新增忽略 `.workbuddy/shots/`）。
- **P3**：`ImportReport.uncategorized` 语义改为「只统计真正入库的未匹配行」（此前重复跳过也计入 →
  0 新增时报「未匹配分类 167 笔」）；报告框零新增时标题改「没有新增」，正文「这 N 笔之前已经导入过了，没有重复记账。」，
  抽出 `_reportText()` 按 `hasNew` 分叉，且空行/未匹配分类/月份引导按条件输出。
- **P4 真正根因**：MuMu **横屏 1600×900** 下 hero(~250) + 预算占位卡(~300) 吃满首屏，
  「本月账单」header 已是 y=737 → 空态整块落到折叠下方。改成**紧凑单行**
  （图标 + 两行说明 + TextButton「记一笔」+ 导入指路），修正原文案「点底部的 ＋」（底栏根本没有 ＋，是「记一笔」）。
- **P5**：`record_page.dart` AppBar 加常驻 `TextButton('保存')`（底部按钮保留）。
- **P6**：预览头补「勾选的条目会导入，点条目可取消它」+「全选 / 全不选」（`allSelected` 取自 activeCount==length）。
- 新增 5 条回归测试 → 门禁 **151/151**、analyze 0。
- **冒烟（MuMu 竖屏 900×1600）**：P4 三项全可见 / P5 AppBar「保存」可见且空金额提示「请输入金额」/
  P6 说明+全不选可见（335→327）/ P3 二次导入「没有新增 + 重复跳过 327 笔」无矛盾数字 / P1 未回归（跳 7 月 417.92）。
- **F6 文档收尾**：README 进度表更新至 F5 完成、技术选型补全并纠正 `excel`→`archive`+手写正则；
  新建 `CHANGELOG.md`（Keep a Changelog 格式，F0–F5 + Unreleased）；`tasks/todo-flutter.md` 补 F5.5/F6/F7。

### 复用坑（本会话新增）
- **`find.widgetWithText(AppBar, '保存')` 返回的是 AppBar 本身**，直接 `tester.tap()` 会点标题区、不触发按钮
  → 点击用 `find.text('保存')`，前者只用于断言「在 AppBar 里」。
- **`pumpAndSettle` 会把 SnackBar 的自动消失定时器一起推进** → 断言 SnackBar 文案要用
  `pump()` + `pump(Duration(milliseconds: 300))`。
- 首页列表项在 800×600 视口常在折叠下方 → `ensureVisible` 对小屏不够稳，改用
  `tester.dragUntilVisible(finder, find.byType(CustomScrollView), Offset(0, -120))`。
- **MuMu 会自己切横竖屏**：同一会话内坐标全变（1600×900 → 900×1600），
  写死坐标的 adb 脚本每步都要先 `wm size` / 重新 dump 拿坐标。

## 第二轮 first-run-acceptance 复走（02:47–02:55）
- 用户指令「跑一轮」→ 用 `first-run-acceptance` 规范重走 M1/M2（MuMu 12 竖屏 900×1600、`pm clear` 零配置）。
- **结论：M1/M2 均 0 阻断，P1–P6 修复全部 hold，六维度（入口可懂/运行前缺项/状态保留/等待反馈/失败可懂/结果可取用）全过。**
- 关键实测：
  - M1：记一笔 88.88 + 餐饮 → 首页即时出现「支出 88.88 / 结余 -88.88」；未选分类直接保存被内联拦「请选择分类」✅
  - M2：导入微信 335→327 → 报告「成功导入 327 笔 / 未匹配分类 167 笔（暂归其他）」；点「去看账单」→ 落首页 + 自动翻 **7月·支出 417.92**
  - P3：二次导入同一文件 → 报告标题「没有新增」+「这 327 笔之前已经导入过了，没有重复记账。」+「重复跳过 327 笔」，无「成功导入 0 笔」/「未匹配分类」矛盾 ✅
  - P6：预览页「勾选的条目会导入，点条目可取消它」+「全不选」；点首条 327→326→327 计数实时同步 ✅
  - 维度3 持久化：force-stop 重启后 9月 88.88 仍在 ✅
- 观察项（非阻断）：首导「未匹配分类 167 笔」是真实入库计数（规则集覆盖有限，未命中暂归其他），措辞已诚实标注。
- 回写：`docs/acceptance-M1-M2.md` 第 11 节（第二轮验收）；`HANDOFF.md` 验收节补第二轮结论、M2 结论改 ✅、未解决问题第 4 条改【P2 已解】、顶行改「F1–F6 ✅ / F7 未排期」。
- 证据截图：`mumu-04-acceptance-round2.png`（重启后 9月 88.88）、`mumu-05-july-round2.png`（7月 417.92）。
- 坑：记一笔金额键盘 `8` 只在 (800,594)，(800,687) 是 `0` —— 坐标写死脚本录入 88.88 要用 5×(800,594)+1×(279,687)。
