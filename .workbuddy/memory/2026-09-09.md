# 2026-09-09

## F3 数据层完成（drift）

- 提交 `7a498d8`（代码）+ HANDOFF 更新，已推远端（`c49d46c`）。
- 新增：`lib/core/db/{tables,database,database.g.dart,schema_v1}.dart`、`lib/core/constants/preset.dart`、
  `lib/data/repositories/{book,account,category,transaction}_repository.dart`。
- 门禁：`flutter analyze` No issues found；`flutter test` 56/56（F2 27 + F3 新增 29）。
- 关键决策（已写入 ADR-9）：
  1. 表结构交给 drift 声明式生成，**7 条索引走原始 SQL**（`kSchemaV1Indexes` 在 `onCreate` 执行）——
     drift `@TableIndex` 不支持 `DESC` 和部分索引 `WHERE`。
  2. `Transactions` 数据类名改 `TxRow`（`@DataClassName`），避开 drift 自带 `Transaction`。
  3. 版本号归 drift（`PRAGMA user_version`），`schema_meta` 只做 KV（`active_book_id`）。
  4. 不写 `BaseRepository` 泛型基类；4 个 repo 各自实现（drift companion 已类型安全）。
  5. `importTransaction` 先查后插判重（不解析 SqliteException 消息），返回 `ImportResult` sealed。
- 未做：SPEC 列的 `lib/core/result.dart`（`Result<T>`）——当前无调用方，F4 需要时再引入。
- 下一步：F4 UI 基础（features/ledger、record、category、book），M1 等价 8 项验收。


## F4 UI 基础代码完成（M1 等价）

- 提交 `cf0580b`，已推远端。APK 门禁通过（增量构建 1m47s）。
- 新增：`lib/core/providers/`（DI + 当前账本状态）、`lib/features/{ledger,record,book,category,shared}/`。
- 门禁：analyze 0 issue；`flutter test` **68/68**（新增 6 条 widget 测试走完整 UI 路径）。
- F4 五坑（已记入 HANDOFF + 用户级 MEMORY）：
  1. Riverpod 3 未导出 `Override` 类型；`AsyncValue.valueOrNull` 已删（用 `.value`）。
  2. go_router 实例必须放 App State，顶层 final 会让测试共享导航状态互相污染。
  3. showDialog 里的 TextField+controller 必须 StatefulWidget（无状态 build 重建丢输入）。
  4. widget 测试写库是真实异步 → pumpUntil(finder) 轮询，别只 pumpAndSettle。
  5. 测试视口 800×600 偏小 → record 页改 SingleChildScrollView + ensureVisible 再 tap。
- 下一步：**F4 真机验收 8 项**（Redmi K50），过后 F5。


## F4 真机验收通过 + F4.5 首页改版（2026-09-10 凌晨）

- 用户确认 F4 M1 等价 8 项真机验收通过。
- F4.5（提交 `74f9d2c`，已推远端）：全局深色主题（#0C0C0C 底 + #FFAF38 橙）、
  StatefulShellRoute 4 tab 底部导航 + 中央橙「记一笔」、首页按 home_ui.jpg 重构
  （header 账本名开抽屉 / 琥珀 hero 月支出卡 / 预算纯占位卡 / 本月账单列表）、
  账本抽屉（当前高亮 + 管理账本）。analyze 0 issue，test **69/69**，APK 构建过。
- **坑 6（关键）**：go_router `context.push` 返回的 Future 在 StatefulShellRoute 壳下
  **不兑现 .then 回调**（真机/测试均不复现结果）→ 保存后刷新必须由记一笔页在 pop 前
  自己 `refresh()`，调用方 `.then` 链路全部移除。
- 测试适配细节：pumpUntil 会命中对话框 EditableText（先等 TextField 消失再断言）；
  pageBack 只认 Cupertino 返回键（Material AppBar 用 find.byIcon(Icons.arrow_back)）；
  深色下流水配色 支出 #EF5350 / 收入 #66BB6A。
- 设计决策（用户已默许）：流水列表保留（核心功能只换皮）；预算卡纯静态占位；
  分类管理入口挪到「我的」页；占位点击 SnackBar「建设中」。
- 下一步：F4.5 真机验收 4 项 → F5 账单导入。


## 记一笔 push 卡顿修复（perf，提交 f060c88）

- 用户反馈：点「记一笔」转场中间明显卡顿。
- 基线测量（widget 帧序列 + Spy 仓储计数器）：push 时 listByBook 重复查 1 次 +
  首帧 spinner 占位、完整页在动画中途二次 build → 卡顿根因是 initState 异步门闩。
- 修复：分类改 watch 已缓存 categoriesProvider（首页已就绪，push 0 新查询），
  首帧即完整页；编辑模式 tx 详情异步填充不挡渲染。
- 复测同口径：listByBook 2→1、首帧 fullPage=true。门禁 69/69 + analyze 0 issue。
- 测量方法论（可复用）：SpyCategoryRepository 计数 + pump 后逐帧查 finder，
  widget 级能测「重复查询/中途换内容」，GPU 栅格化开销需真机 profile 模式。
